class node:
    def __init__(self, id, left_child=None, right_child=None):
        self.id = id
        self.left_child = left_child
        self.right_child = right_child

    def is_leaf(self):
        return left_child is None and right_child is None

    def children():

    def generate_element(self):


class tree:
    def __init__(self, sklearn_tree=None, json_object=None):
        if tree_info is not None:
            self.node_count: sklearn_tree.tree_.node_count,
            self.children_left: sklearn_tree.tree_.children_left.tolist(),
            self.children_right: sklearn_tree.tree_.children_right.tolist(),
            self.value: sklearn_tree.tree_.value.tolist(),
            self.feature: sklearn_tree.tree_.feature.tolist(),
            self.threshold: sklearn_tree.tree_.threshold.tolist(),
            self.max_depth: sklearn_tree.tree_.max_depth

            self.nodes = []
            for node_id in range(self.node_count):

    def generate():

